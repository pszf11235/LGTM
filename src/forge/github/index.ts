/**
 * The GitHub ForgeAdapter: adapter.ts (REST calls, ETags) and
 * draft-review.ts (PENDING review creation, minus submit) land here.
 * Ported from packages/plugins/review/src/infra/github.ts and
 * domain/pending-review.ts on the old `main` branch.
 */
export {};
