/**
 * ForgeAdapter implementations. GitHub lives in ./github; a GitLab or other
 * host would sit here as a sibling, behind the same ForgeAdapter interface
 * from @/core/types (design.md, "ForgeAdapter").
 *
 * Empty for now — the GitHub adapter is a separate porting task (see
 * docs/spec/design.md, "Ported modules": infra/github.ts and
 * domain/pending-review.ts).
 */
export {};
